<div class="border border-line rounded-[4px] bg-row p-3.5 shadow-sm">
  <div class="text-[10px] font-bold text-muted uppercase tracking-wide"><%= label %></div>
  <div class="text-[22px] font-bold mt-1 <%= color ? "" : "text-ink" %>" style="<%= "color: #{color};" if color %>"><%= value %></div>
</div>
